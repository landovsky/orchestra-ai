<%# 
  DEPRECATED: This layout is maintained for backward compatibility only.
  New code should use 'authenticated' layout for authenticated pages or 'base' for unauthenticated pages.
%>
<%= render layout: 'layouts/authenticated' do %>
  <%= yield %>
<% end %>
